services:
  db:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: smartpathy
    ports: ["3308:3306"]
    volumes: ["db_data:/var/lib/mysql"]
volumes:
  db_data:
